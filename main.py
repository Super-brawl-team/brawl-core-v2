# main.py

import sys
import time
import subprocess
import pyautogui as pg
from threading import Thread


class Main:

    class run(Thread):
        def run(self) -> None:
            def check(self) -> None:
                print("By XidMods05")
                print("This core has the best AntiDDos ever leaked.")
                print("This core has the best technology that has ever been merget into open-source.")
                print("\n")
                if __name__ == "__main__":
                    class handler(Thread):
                        subprocess.Popen(["start", "cmd"], shell=True).wait()
                        time.sleep(2)
                        pg.press("enter")
                        pg.typewrite("python handler.py")
                        pg.press("enter")
                    hd = handler()
                    hd.start()
                    print("Launcher tested! @main. \n")
                else:
                    print("Not main or launcher hack! - " + __name__)
                    sys.exit(1001)
            check(self)
    r = run()
    r.start()












